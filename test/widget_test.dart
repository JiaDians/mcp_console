import 'package:flutter_test/flutter_test.dart';

void main() {
  testWidgets('App smoke test', (WidgetTester tester) async {
    // Smoke test placeholder — UI tests require full provider setup.
    expect(true, isTrue);
  });
}
